# Tao Shi — Personal Academic Homepage

Static academic homepage for [taoshi1999.github.io](https://taoshi1999.github.io).

The site is built with plain HTML, CSS, and JavaScript and is deployed automatically by GitHub Pages from the `main` branch.

## Local preview

```powershell
python -m http.server 8000
```

Then open `http://localhost:8000`.
